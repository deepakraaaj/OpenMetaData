# Alembic

This scaffold is included so the project can evolve toward a durable application metadata store if needed.

The initial implementation writes normalized and semantic outputs to versionable filesystem artifacts under `output/`.
